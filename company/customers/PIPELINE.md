<!-- COCKPIT generato da scripts/generate-pipeline.py (o /sales board). NON modificare a mano: rigenera. -->

# 📊 Pipeline — Cockpit Commerciale

> Nessuna opportunità ancora. Crea trattative in `company/customers/opportunities/` (da `TEMPLATE.md`),
> poi genera il cockpit:
>
> ```
> python scripts/generate-pipeline.py
> ```
>
> oppure `/sales board`. Vedi un esempio completo in `examples/acme-demo/customers/PIPELINE.md`.
> Configurazione (target, segmenti, soglie aging): `company/customers/pipeline-config.yaml`.
