# 📑 Spec Index

> Indice delle specifiche prodotto. Gestito dal PM e dallo Spec Lifecycle Protocol
> (`system/protocols/spec-lifecycle.md`). Ogni spec ha uno stato nel frontmatter.

| Spec | Stato | Decisione | ClickUp | Ultimo check |
|------|-------|-----------|---------|--------------|
| — | — | — | — | — |

> Vuoto all'avvio. Le spec vengono create da `/pm write-spec` e salvate in `company/product/specs/`.

## Stati possibili (vedi spec-lifecycle.md)
- `draft` → `evaluated` → `approved` → `in-development` → `shipped`
